---
title: 'Numerical Methods: Assignment 2'
author: ''
date: '2017-08-26'
slug: numerical-methods-assignment-2
categories: []
tags: []
draft: TRUE
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

1.  Let $x=3248$. Use the functions `floor()` and/or `%%` to write an `R` expression that returns the second decimal place in $x$. Make sure your code also works for $x = -3248$.

    ```{r}
    # notice the spacing of this chunk...what is its purpose? 
    x <- 3248
    # write question 1 code inside this chunk
    
    ```

2.  **(2.10.2.d)** Write an `R` expression that returns the following matrix.  
$\left(\begin{array}{rrr}0 & 2 & 3 \\0 & 5 & 0 \\7 & 0 & 0\end{array}\right)$

    ```{r}
    # write question 2 code inside this chunk
    ```

3.  **(2.10.4)**

4.  